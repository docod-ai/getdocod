---
name: loop
description: Dispatch one DOCOD task through build, QA, fixes, and review. Invoke only for an explicit /docod:loop request.
---

Require an installed `.docod/` bundle, preserve the user's arguments, then read
`.docod/adapter-assets/codex/commands/loop.md` in full and follow it exactly.
