---
name: continue
description: Resume a DOCOD workstream. Invoke only for an explicit /docod:continue request.
---

Require an installed `.docod/` bundle, preserve the user's arguments, then read
`.docod/adapter-assets/codex/commands/continue.md` in full and follow it exactly.
