---
name: start
description: Run the DOCOD entry-point command. Invoke only for an explicit /docod:start request.
---

Require an installed `.docod/` bundle, then read
`.docod/adapter-assets/codex/commands/start.md` in full and follow it exactly.
