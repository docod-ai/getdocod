interface:
  display_name: "DOCOD Workstreams"
  short_description: "Operate the governed workstream registry."
  default_prompt: "Use $ws for an explicit /docod:ws request."
policy:
  allow_implicit_invocation: false
