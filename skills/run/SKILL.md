---
name: run
description: Invoke one governed DOCOD agent action. Invoke only for an explicit /docod:run request.
---

Require an installed `.docod/` bundle, preserve the user's arguments, then read
`.docod/adapter-assets/codex/commands/run.md` in full and follow it exactly.
