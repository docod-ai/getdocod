---
name: approve
description: Record a human DOCOD approval. Invoke only for an explicit /docod:approve request; never infer approval.
---

Require an installed `.docod/` bundle, preserve the user's arguments, then read
`.docod/adapter-assets/codex/commands/approve.md` in full and follow it exactly.
