# `/docod:ws <operation>`

Run `node .docod/docod.mjs ws <arguments>`. `abandon` requires `--reason` and
must refuse without it. Present filesystem-derived output; do not decide a
workstream's disposition for the user.

