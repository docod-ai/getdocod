# `/docod:report`

Run `node .docod/docod.mjs report`. Present the generated `.docod/report.html`
path and offer to open it when the current Codex surface supports that action.
The report is a static view of now; tell the user to regenerate it after state
changes.

