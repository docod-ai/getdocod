# `/docod:status`

Run `node .docod/docod.mjs status` and present the output without softening it.
An invalid-approval, stale-input, or queue warning appears as-is: status does
not reassure; it reports the derived filesystem state.

